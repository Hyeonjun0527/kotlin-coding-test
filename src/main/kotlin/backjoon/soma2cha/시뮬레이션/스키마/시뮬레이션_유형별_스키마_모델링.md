# 시뮬레이션 유형별 스키마 모델링

## 결론

아래 9개 문제는 모두 모델링 가능하다.
- 엘레베이터
- 파이어볼
- 거북이
- 디지털티비
- 주차장
- 표편집
- 지뢰찾기
- 안전지대
- 오목

단, 뼈대는 하나로 고정하지 않고 유형별로 나눠야 읽기 쉽고 재사용이 된다.

## 문제 유형 분리 정책

| 유형 | 핵심 상태축 | 반복 골격 | 해당 문제 |
|---|---|---|---|
| 시간축 이벤트 | 시간, 미래요청, 대기요청, 시스템상태 | 종료검사 -> 유입 -> 처리 -> 분기 -> 이동/시간증가 | 엘레베이터 |
| 라운드 동시갱신 | 라운드, 현재상태, 다음버킷 | 이동 -> 집계 -> 합치기/분리 -> 확정 | 파이어볼 |
| 명령 재생 | 명령포인터, 커서/좌표, 방향 | 명령읽기 -> 규칙적용 -> 상태갱신 | 거북이, 디지털티비 |
| 자원할당 큐 | 이벤트스트림, 가용자원, 대기큐 | 이벤트처리 -> 자원할당/회수 -> 대기해소 | 주차장 |
| 링크구조 편집 | 위/아래 링크, 커서, 삭제스택 | 명령처리 -> 링크재연결 | 표편집 |
| 격자 로컬규칙 | 입력격자, 출력격자, 인접방향 | 전체순회 -> 인접계산 -> 출력반영 | 지뢰찾기, 안전지대 |
| 패턴 스캔 | 보드, 스캔포인터, 방향벡터 | 후보탐색 -> 연속검사 -> 양끝검증 | 오목 |

---

## 문제별 모델 카드 (S/E/T)

아래는 각 문제를 상태(State) / 이벤트(Event) / 전이(Transition)로 바로 적용한 카드다.

| 문제 | 유형 | 상태 S | 이벤트 E | 전이 T |
|---|---|---|---|---|
| 엘레베이터 | 시간축 이벤트 | `현재시간, 현재층, 방향, 요구큐, 대기중인층` | `시간도달, 현재층도착, 벽도달` | `미래->대기`, `대기->처리`, `방향전환/유지`, `이동` |
| 파이어볼 | 라운드 동시갱신 | `fireballs, buckets, round` | `라운드시작, 셀집계` | `이동`, `합치기`, `분리`, `소멸` |
| 거북이 | 명령 재생 | `x,y,dir,min/max` | `명령 L/R/F/B` | `회전`, `전진/후진`, `경계박스 갱신` |
| 디지털티비 | 명령 재생 | `채널리스트, 커서, 출력버튼로그` | `목표채널 탐색/스왑` | `커서이동`, `스왑`, `버튼기록` |
| 주차장 | 자원할당 큐 | `빈칸PQ, 대기차Q, 차->칸 매핑, 수입` | `입차, 출차` | `즉시배정/대기`, `반납`, `대기해소`, `요금누적` |
| 표편집 | 링크구조 편집 | `위/아래 링크, 커서, 삭제스택, 상태문자열` | `U,D,C,Z` | `커서이동`, `삭제/연결`, `복구/연결` |
| 지뢰찾기 | 격자 로컬규칙 | `mineMap, openMap, output` | `칸열기, 폭발여부` | `8방향 카운트`, `폭발시 지뢰 공개` |
| 안전지대 | 격자 로컬규칙 | `board, danger` | `지뢰칸 발견` | `주변 9칸 위험표시`, `안전칸 카운트` |
| 오목 | 패턴 스캔 | `board, 방향벡터, 후보좌표` | `돌칸 발견` | `시작점검증`, `연속5검사`, `양끝검증` |

---

## 1) 시간축 이벤트 뼈대 (엘레베이터)

```mermaid
graph TB
  S0["초기화: 시간, 층, 방향, 큐/대기 상태"]
  S1["루프 시작"]
  S2["종료 검사: 미래요청/대기요청 모두 비었는가"]
  S3["유입: 현재시간 요청을 미래->대기로 이동"]
  S4["현재층 즉시 처리"]
  S5["대기요청 비었는가"]
  S6["정지: 이동 없이 시간 +1"]
  S7["방향 결정"]
  S8["벽 보정"]
  S9["1층 이동 + 시간 +1"]
  S_END["종료"]

  S0 --> S1 --> S2
  S2 -- 예 --> S_END
  S2 -- 아니오 --> S3 --> S4 --> S5
  S5 -- 예 --> S6 --> S1
  S5 -- 아니오 --> S7 --> S8 --> S9 --> S1
```

---

## 2) 라운드 동시갱신 뼈대 (파이어볼)

```mermaid
graph TB
  S0["초기 파이어볼 상태"]
  S1["라운드 시작 (1..K)"]
  S2["모든 파이어볼 이동 후 버킷에 적재"]
  S3["버킷 순회"]
  S4["1개면 유지"]
  S5["2개 이상이면 합치기/분리 규칙 적용"]
  S6["질량 0이면 소멸, 아니면 4개 생성"]
  S7["다음 라운드 상태 확정"]
  S8["K라운드 종료 후 총 질량 계산"]

  S0 --> S1 --> S2 --> S3
  S3 --> S4 --> S7
  S3 --> S5 --> S6 --> S7
  S7 --> S1
  S1 -- 라운드 완료 --> S8
```

---

## 3) 명령 재생 뼈대 (거북이, 디지털티비)

```mermaid
graph TB
  S0["초기 상태: 커서/좌표/방향"]
  S1["명령 1개 읽기"]
  S2["명령 해석"]
  S3["이동/회전/스왑 등 상태 갱신"]
  S4["명령 남음?"]
  S5["최종 상태 출력"]

  S0 --> S1 --> S2 --> S3 --> S4
  S4 -- 예 --> S1
  S4 -- 아니오 --> S5
```

---

## 4) 자원할당 큐 뼈대 (주차장)

```mermaid
graph TB
  S0["초기화: 가용칸 PQ, 대기차 Queue, 차->칸 매핑"]
  S1["이벤트 1개 읽기"]
  S2["입차 이벤트인가"]
  S3["빈칸 있으면 즉시 배정, 없으면 대기열"]
  S4["출차 처리: 요금합산, 칸 반납"]
  S5["대기차 있으면 즉시 배정"]
  S6["이벤트 남음?"]
  S7["최종 수입 출력"]

  S0 --> S1 --> S2
  S2 -- 예 --> S3 --> S6
  S2 -- 아니오 --> S4 --> S5 --> S6
  S6 -- 예 --> S1
  S6 -- 아니오 --> S7
```

---

## 5) 링크구조 편집 뼈대 (표편집)

```mermaid
graph TB
  S0["초기화: 위/아래 링크, 커서, 삭제스택"]
  S1["명령 1개 읽기"]
  S2["U/D: 링크 따라 커서 이동"]
  S3["C: 현재행 삭제 + 링크 재연결 + 스택 push"]
  S4["Z: 스택 pop + 링크 복구"]
  S5["명령 남음?"]
  S6["상태 문자열 반환"]

  S0 --> S1
  S1 --> S2 --> S5
  S1 --> S3 --> S5
  S1 --> S4 --> S5
  S5 -- 예 --> S1
  S5 -- 아니오 --> S6
```

---

## 6) 격자 로컬규칙 뼈대 (지뢰찾기, 안전지대)

```mermaid
graph TB
  S0["입력 격자 로드"]
  S1["출력/위험 격자 초기화"]
  S2["모든 칸 순회"]
  S3["해당 칸이 규칙 트리거인가"]
  S4["인접칸(8방향 또는 9칸) 계산"]
  S5["출력 칸 갱신"]
  S6["순회 완료"]
  S7["최종 출력/개수 반환"]

  S0 --> S1 --> S2 --> S3
  S3 -- 예 --> S4 --> S5 --> S2
  S3 -- 아니오 --> S2
  S2 -- 완료 --> S6 --> S7
```

---

## 7) 패턴 스캔 뼈대 (오목)

```mermaid
graph TB
  S0["보드 로드"]
  S1["모든 칸을 시작점 후보로 순회"]
  S2["돌이 있으면 방향 후보 검사"]
  S3["시작점 검증: 반대칸 같은 색이면 스킵"]
  S4["한 방향 연속 길이 검사"]
  S5["정확히 5개 + 다음칸 다름 검증"]
  S6["승자 확정 및 좌표 출력"]
  S7["없으면 0 출력"]

  S0 --> S1 --> S2 --> S3 --> S4 --> S5
  S5 -- 만족 --> S6
  S5 -- 불만족 --> S1
  S1 -- 전체완료 --> S7
```
