
# Kotlin Sorting 정리표

## 🔹 제자리 정렬 (원본 변경)

| 종류 | 함수명                  | 원본 변경 | 반환값 | 설명         |
|------|----------------------|-----------|--------|------------|
| 제자리 정렬 | sort()               | ✔ | Unit | 기본 오름차순 정렬 |
| 제자리 정렬 | sortDescending()     | ✔ | Unit | 기본 내림차순 정렬 |
| 제자리 정렬 | sortBy { }           | ✔ | Unit | key 기준 오름차순 |
| 제자리 정렬 | sortByDescending { } | ✔ | Unit | key 기준 내림차순 |
| 제자리 정렬 | sortWith(comparator) | ✔ | Unit | 복합 조건 정렬   |

---

## 🔹 새 리스트 정렬 (원본 유지)

| 종류 | 함수명 | 원본 변경 | 반환값 | 설명 |
|------|--------|-----------|--------|-------|
| 새 리스트 정렬 | sorted() | ✘ | 새 List | 기본 오름차순 정렬 |
| 새 리스트 정렬 | sortedDescending() | ✘ | 새 List | 기본 내림차순 정렬 |
| 새 리스트 정렬 | sortedBy { } | ✘ | 새 List | key 기준 오름차순 |
| 새 리스트 정렬 | sortedByDescending { } | ✘ | 새 List | key 기준 내림차순 |
| 새 리스트 정렬 | sortedWith(comparator) | ✘ | 새 List | 복합 조건 정렬 |

---

# Comparator 관련

| 이름 | 예시 | 설명 |
|------|------|-------|
| compareBy | compareBy { it.age } | 오름차순 기준 하나 |
| compareByDescending | compareByDescending { it.score } | 내림차순 기준 하나 |
| thenBy | thenBy { it.name } | 두 번째 기준 (오름차순) |
| thenByDescending | thenByDescending { it.value } | 두 번째 기준 (내림차순) |

---

# 핵심 기억 포인트
- **sort = 원본 변경**
- **sorted = 원본 유지, 새 리스트 반환**
- **By = key 기반 정렬**
- **With = comparator 직접 지정**

---

# 📘 사용 예시 모음 (실제 코드)

## 1) 제자리 정렬

### ✔ 기본 오름차순
```kotlin
val list = mutableListOf(3, 1, 2)
list.sort()
println(list)  // [1, 2, 3]
````

### ✔ key 기준 오름차순

```kotlin
list.sortBy { it.length }
```

### ✔ key 기준 내림차순

```kotlin
list.sortByDescending { it.score }
```

### ✔ 복합 조건

```kotlin
list.sortWith(
    compareByDescending<Person> { it.score }
        .thenBy { it.age }
)
```

---

## 2) 새 리스트 정렬

### ✔ 기본 오름차순

```kotlin
val sortedList = list.sorted()
```

### ✔ 내림차순 전체

```kotlin
val sortedList = list.sortedDescending()
```

### ✔ key 기준 오름차순

```kotlin
val sortedList = list.sortedBy { it.time }
```

### ✔ key 기준 내림차순

```kotlin
val sortedList = list.sortedByDescending { it.value }
```

### ✔ 복합 조건 정렬

```kotlin
val sortedList = list.sortedWith(
    compareByDescending<Item> { it.price }
        .thenBy { it.name }
)
```

---

## 3) PriorityQueue에서 comparator 사용

```kotlin
val pq = PriorityQueue(
    compareByDescending<Pair<Int,Int>> { it.first }
        .thenBy { it.second }
)

pq.add(3 to 1)
pq.add(3 to 0)
pq.add(5 to 7)
```

---

## 4) data class 정렬 예시

```kotlin
data class User(val score: Int, val age: Int)

val users = listOf(
    User(50, 20),
    User(50, 18),
    User(70, 21)
)

val sortedUsers = users.sortedWith(
    compareByDescending<User> { it.score }
        .thenBy { it.age }
)
```

---

# ✨ 한 줄 요약

정렬에서 헷갈리면 이렇게만 기억하자:

> **sort = 바꾼다, sorted = 새로 만든다, By = 기준, With = 비교기 직접 전달.**

```

---

필요하면 **(1) 최빈 정렬 패턴 모음, (2) 랭킹/스코어 정렬 전용 템플릿, (3) 그래프 정렬 패턴**도 따로 만들어줄게!
```

# **📘 Kotlin Sorting / PQ 실전 연습 예시들**

---

## ✔️ 예시 1: 리스트를 오름차순으로 정렬해라 (원본 변경)

```kotlin
val list = mutableListOf(5, 3, 1, 4)
list.sort()
println(list) // [1,3,4,5]
```

---

## ✔️ 예시 2: 리스트를 내림차순으로 정렬해라 (원본 변경)

```kotlin
val list = mutableListOf(5, 3, 1, 4)
list.sortDescending()
println(list) // [5,4,3,1]
```

---

## ✔️ 예시 3: 문자열 리스트를 길이 기준으로 오름차순 정렬해라

```kotlin
val list = mutableListOf("banana", "kiwi", "apple")
list.sortBy { it.length }
println(list) // [kiwi, apple, banana]
```

---

## ✔️ 예시 4: 문자열 리스트를 길이 기준으로 내림차순 정렬해라

```kotlin
val list = mutableListOf("banana", "kiwi", "apple")
list.sortByDescending { it.length }
println(list) // [banana, apple, kiwi]
```

---

## ✔️ 예시 5: User(score, age)를 score 내림차순 → age 오름차순으로 정렬해라

```kotlin
data class User(val score: Int, val age: Int)

val users = mutableListOf(
    User(90, 20),
    User(90, 18),
    User(75, 30)
)

users.sortWith(
    compareByDescending<User> { it.score }
        .thenBy { it.age }
)

println(users)
```

---

## ✔️ 예시 6: 원본을 건드리지 않고 새 리스트로 오름차순 정렬해라

```kotlin
val list = listOf(5, 1, 3)
val sorted = list.sorted()
println(sorted) // [1,3,5]
println(list)   // [5,1,3]
```

---

## ✔️ 예시 7: 원본을 건드리지 않고 새 리스트로 내림차순 정렬해라

```kotlin
val list = listOf(5, 1, 3)
val sorted = list.sortedDescending()
println(sorted) // [5,3,1]
```

---

## ✔️ 예시 8: Item(price, name)을 price 내림차순 → name 오름차순 정렬해라 (새 리스트)

```kotlin
data class Item(val price: Int, val name: String)

val items = listOf(
    Item(100, "c"),
    Item(100, "a"),
    Item(50, "b")
)

val sorted = items.sortedWith(
    compareByDescending<Item> { it.price }
        .thenBy { it.name }
)

println(sorted)
```

---

## ✔️ 예시 9: PriorityQueue에서 큰 숫자가 먼저 나오게 만들어라

```kotlin
val pq = PriorityQueue(compareByDescending<Int> { it })

pq.add(3)
pq.add(10)
pq.add(1)

println(pq.poll()) // 10
println(pq.poll()) // 3
println(pq.poll()) // 1
```

---

## ✔️ 예시 10: PriorityQueue에서 Pair(score, time)를 score 내림차순 → time 오름차순으로 나오게 해라

```kotlin
val pq = PriorityQueue(
    compareByDescending<Pair<Int,Int>> { it.first }
        .thenBy { it.second }
)

pq.add(90 to 5)
pq.add(90 to 3)
pq.add(80 to 10)

println(pq.poll()) // (90, 3)
println(pq.poll()) // (90, 5)
println(pq.poll()) // (80, 10)
```

---

## ✔️ 예시 11: 원본 리스트는 그대로 두고, 길이 기준 내림차순 정렬된 새 리스트를 만들어라

```kotlin
val list = listOf("apple", "kiwi", "banana")
val result = list.sortedByDescending { it.length }

println(result) // [banana, apple, kiwi]
println(list)   // 원본 그대로
```

---

## ✔️ 예시 12: null 허용 문자열 리스트에서 null을 뒤로 보내고 정렬해라

```kotlin
val list = mutableListOf("apple", null, "banana", null, "kiwi")

list.sortWith(
    compareBy<String?> { it == null }
        .thenBy { it }
)

println(list) // [apple, banana, kiwi, null, null]
```
