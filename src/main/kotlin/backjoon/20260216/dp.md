
```kotlin
// DP 수도코드 (Bottom-Up: 테이블 채우기)

초기상태
dp = /* 테이블 */
dp[기저상태] = 기저값

for (상태 in 작은상태_큰상태_순서) {
    dp[상태] = 초기값
    for (선택 in 가능한선택들(상태)) {
        val 다음상태 = 전이(상태, 선택)
        dp[상태] = 결합(dp[상태], dp[다음상태])
    }
}

print(dp[시작상태])

```