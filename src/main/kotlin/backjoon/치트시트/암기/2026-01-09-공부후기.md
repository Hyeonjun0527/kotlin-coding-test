# 2026-01-09 공부 후기

## 소수점 반올림

### 출력할 때 joinToString하면서 소수점 3자리 반올림?
```kotlin
val nums = listOf(1.23456, 7.89123)
val out = nums.joinToString(" ") { "%.3f".format(it) }
println(out) // 1.235 7.891
```

### 출력할 때 소수점 3자리 반올림?
```kotlin
val x = 1.23456
println("%.3f".format(x)) // 1.235
```

### 값 자체를 소수점 3자리로 반올림?
"숫자로 만들기"
```kotlin
import kotlin.math.round

val x = 1.23456
val rounded = round(x * 1000) / 1000
println(rounded) // 1.235
```

## 리스트 조작

### 리스트 요소를 진짜 7개만 남기고 제거?
"원본 변경"
```kotlin
val list = mutableListOf(1,2,3,4,5,6,7,8,9)

while (list.size > 7) {
    list.removeLast()
}
println(list) // [1,2,3,4,5,6,7]
```

### 리스트에서 마지막 요소 꺼내기?
"pop"
```kotlin
val list = mutableListOf(1, 2, 3)
val last = list.removeLast() // 꺼내면서 제거
println(last) // 3
```

### 리스트 요소 제거 안 하고 7개만 가져오기?
"원본 유지"
```kotlin
val list = listOf(1,2,3,4,5,6,7,8,9)
val picked = list.take(7)

println(picked) // [1,2,3,4,5,6,7]
println(list)   // 원본 그대로
```

## 리스트 정렬

### 내림차순 정렬?
"원본 변경"
```kotlin
val nums = mutableListOf(5, 1, 9, 3)
nums.sortDescending()
println(nums) // [9,5,3,1]
```

### key 기준으로 내림차순?
"예: 길이 기준"
```kotlin
val words = mutableListOf("apple", "kiwi", "banana")
words.sortByDescending { it.length }
println(words) // [banana, apple, kiwi]
```

## PriorityQueue 순회

### PQ를 내림차순으로 만들어 poll()로 순회?
"큰 값 먼저"
```kotlin
val pq = PriorityQueue(compareByDescending<Int> { it })

pq.addAll(listOf(10, 3, 7, 1))

while (pq.isNotEmpty()) {
    println(pq.poll())
}
// 10
// 7
// 3
// 1
```

### PQ를 정렬된 순서대로 순회하되 원본 유지?
"복사본 만들기"
```kotlin
val pq = PriorityQueue(compareByDescending<Int> { it })
pq.addAll(listOf(10, 3, 7, 1))

val copy = PriorityQueue(pq) // 복사본

while (copy.isNotEmpty()) {
    println(copy.poll())
}
// 원본 pq는 그대로 유지됨
```
