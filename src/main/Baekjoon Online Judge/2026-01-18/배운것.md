회문인수는 꼭 다시 풀어보자.
### 진법으로 나누는 코드
```kotlin
while (n > 0) {
    list.add(n % it)
    n /= it
}
list.reverse()
```
### .any와 .all은 어케 써?

✔️ 예시 1: 리스트 안에 짝수가 하나라도 있나?
```kotlin
val nums = listOf(1, 3, 5, 8)
val hasEven = nums.any { it % 2 == 0 }
println(hasEven) // true
```

✔️ 예시 2: 문자열에 숫자가 하나라도 있나?
```kotlin
val str = "abc3d"
val containsDigit = str.any { it.isDigit() }

println(containsDigit) // true

```

✔️ 예시 3: 리스트의 모든 숫자가 양수인가?
```kotlin
val nums = listOf(1, 2, 3, 4)

val allPositive = nums.all { it > 0 }

println(allPositive) // true
```
✔️ 예시 4: 비밀번호가 “문자 전부 소문자냐?” 검사
```kotlin
val pwd = "hello"
val isAllLower = pwd.all { it.isLowerCase() }

println(isAllLower) // true
```

### reverse는 제자리정렬?(자신을 바꿔) 아니면 리턴해?

제자리 정렬임.