좋아.
너가 헷갈릴 만한 **map / filter / forEach / onEach / let / also / apply / run** 을
**이름 의미 + 동작 철학 + 언제 쓰는지 + 예제 코드**로 한 번에 정리해줄게.

아주 명쾌하게 "아 이래서 이런 이름이구나" 하고 감이 와야 함.

---

# **✨ 코틀린 함수형 도구 비교표 (완전 핵심만)**

## **1) 컬렉션 범주(map, filter, forEach, onEach)**

| 함수          | 이름 의미          | 반환값             | 리스트 변환 여부 | 언제 사용?            |
| ----------- | -------------- | --------------- | --------- | ----------------- |
| **map**     | map A → B      | **새 리스트 반환**    | 변환함       | 값을 변환하고 싶을 때      |
| **filter**  | 걸러내다           | **새 리스트 반환**    | 변환함       | 조건을 만족하는 요소만 남길 때 |
| **forEach** | 각 요소 *에 대해*    | **Unit(반환 없음)** | 변환 없음     | 출력, 로그 등 사이드 이펙트  |
| **onEach**  | 각 요소 *위에서(on)* | **원본 리스트 그대로**  | 변환 없음     | 사이드 이펙트 + 체이닝 유지  |

---

## **2) 스코프 함수(let, also, apply, run)**

*(this/it 차이, 반환값 차이로 구분됨)*

| 함수        | receiver | 반환값       | 의미                    | 사용 시점              |
| --------- | -------- | --------- | --------------------- | ------------------ |
| **let**   | it       | **람다 결과** | “이 값을 it으로 사용해 변환해줘”  | 값 변환, null-safe 처리 |
| **also**  | it       | **원본 객체** | “부가 작업도 해줘”           | 로그, 디버깅            |
| **apply** | this     | **원본 객체** | “설정값 초기화해줘”           | 객체 초기화 패턴          |
| **run**   | this     | **람다 결과** | “this로 영역 잡고 결과 만들어줘” | 블록 실행 + 결과 생성      |

---

# **🔥 완전 요약 문장 (외우기 쉬운 형태)**

* **map** → “바꾸자”
* **filter** → “골라내자”
* **forEach** → “각자 시켜라”
* **onEach** → “각자 시키고 리스트는 그대로 내놔라”
* **let** → “결과를 만들자(it)”
* **also** → “참고로 이것도 해줘(it)”
* **apply** → “내 설정 좀 해줘(this)”
* **run** → “this로 블록 돌리고 결과줘”

---

# **✨ 실전 예제 코드들**

현준님이 실제로 코테·프로젝트에서 바로 쓸 수 있는 수준으로 만들어줄게.

---

# **1) map / filter / forEach / onEach 예제**

```kotlin
val nums = listOf(1, 2, 3, 4, 5)

// map → 변환
val doubled = nums.map { it * 2 }
// [2, 4, 6, 8, 10]

// filter → 조건 유지
val evens = nums.filter { it % 2 == 0 }
// [2, 4]

// forEach → 사이드 이펙트만
nums.forEach { println("num = $it") }

// onEach → 사이드 이펙트 + 리스트 유지
val result = nums
    .onEach { println("processing $it") }
    .filter { it > 2 }

println(result)
// 결과: [3, 4, 5]
```

---

# **2) let / also / apply / run 실전 예제**

```kotlin
data class User(var name: String, var age: Int)

// let → 값 변환
val nameLength = "현준".let { it.length }

//val length = file.readText()
//    .let { parse(it) }
//    .let { it["name"] }
//    .let { it.length }
//
//val t = file.readText()
//val json = parse(t)
//val name = json["name"]
//val len = name.length
// 2

// also → 부가 작업 (로그 등)
val x = mutableListOf(1, 2, 3).also { println("초기 리스트 = $it") }
// apply → 객체 초기화
val user = User("?", 0).apply {
    name = "현준"
    age = 25
}

// run → 블록 실행 후 결과 반환
val greeting = user.run {
    "Hello, $name ($age)"
}
println(greeting)
// Hello, 현준 (25)
```

---

# **3) BFS 실전에서 onEach + filter 사용 예제**

```kotlin
fun bfs(start: Int, adj: List<List<Int>>, visited: BooleanArray) {
    val dq = ArrayDeque<Int>()
    dq.addLast(start)
    visited[start] = true

    while (dq.isNotEmpty()) {
        val cv = dq.removeFirst()
        print("$cv ")

        adj[cv]
            .filter { !visited[it] }       // 안 간 애들만
            .onEach { visited[it] = true } // 방문 표시
            .forEach { dq.addLast(it) }    // 큐에 넣기
    }
}
```

BFS가 **함수형으로 가장 깔끔하게 표현된 형태**라고 보면 됨.

---

# ✨ 필요한 추가 자료도 만들어줄 수 있음

* “함수형 스타일을 절차형 스타일로 1:1 대응한 표”
* “문제 풀이에서 언제 함수형이 좋은지”
* “스코프 함수 8종 비교”
* “onEach 내부 구현 원리”

말만 해줘!
