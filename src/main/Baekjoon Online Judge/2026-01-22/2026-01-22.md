val max = cnt.drop(1).max()
val realWinner = cnt.indexOf(max)

(1..n).maxBy { cnt[it] }

for (i in 1..n) {
    if (cnt[i] > max) {
    max = cnt[i]
    ans = i
    }
}

removeFirst()와 drop(1)의 차이는?
removeFirst()는 “원본을 바꾸는 수술”이고
drop()은 “복사본 하나 만들어서 보여주기”다.

값으로 인덱스 찾기는?
list.indexOf(값)